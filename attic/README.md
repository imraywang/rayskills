# attic · 低频成员归档

v3（2026-07-28）按真实使用频率收敛：以下 6 个成员自 v1 以来没有产生真实调用记录，与高频的内容生产管线也没有阶段交接，从 `skills/` 移入本目录归档。它们不随 `skills add` 安装，也不出现在 `/ray` 路由表中。

| 成员 | 原职责 |
|---|---|
| `ray-benchmark` | 对标拆解，判断可迁移性 |
| `ray-consult` | 企业 AI 落地咨询：诊断段 + 方案段 |
| `ray-launch` | 落地页 / B2B 站上线全流程 |
| `ray-metrics` | X 账号数据周报与传播规律 |
| `ray-report` | magazine 风格 HTML / PDF 长报告 |
| `ray-vps` | VPS 开荒与节点巡检 |

内容完整保留，方法论没有丢。需要重新启用时，把对应目录移回 `skills/`、在 `/ray` 路由表补一行即可；也可以独立成仓。
